# Sample Paper

This is a tiny public sample used only for smoke testing the Beamer seven-phase pipeline runner.

## Model

Let $X_t$ denote a state variable and let $V(t,x)$ be a value function.

\[
  V_t + \mu x V_x + \frac{1}{2}\sigma^2 x^2 V_{xx} - rV = 0.
\]

## Result

The Beamer deck should explain the model, define symbols before first visible use, and keep equation coverage explicit.
